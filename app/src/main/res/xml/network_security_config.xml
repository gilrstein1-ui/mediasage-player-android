<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Player is served over plain HTTP on the home LAN; allow cleartext. -->
    <base-config cleartextTrafficPermitted="true"/>
</network-security-config>
